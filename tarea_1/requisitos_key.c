#include <stdio.h>
#include <string.h>

void equal_length(char *key1, char *key2){
    int key1_len = strlen(key1);
    int key2_len = strlen(key2);
    if (key1_len != key2_len){
        printf("las key son de distinta longitud, intenta con keys del mismo largo\n");
    }
}

void all_char_different(char *key1, char *key2){
    
}
